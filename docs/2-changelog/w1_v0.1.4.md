# Changelog - Week 1, 07-08-2026, V. 0.1.4

**Release Date**: Week 1, 07-08-2026
**Version**: 0.1.4 (previously 0.1.3)
**Object**: feat(data): 2026-08 patch intake — two new summer 5★ cards, five new songs, regenerated rankings and guides
**Code review**: expedited release-day intake per the repo's update-holodori-meta skill; full verification chain + copy audit green

## Changes

- **Roster**: 113 → 115 cards. New paired summer banner: Sakura Miko
  "Radiant Beach Shot" (`card-00015-5-uniq-0067-00`) and Hoshimachi Suisei
  "Water Gun Arpeggio" (`card-00018-5-uniq-0068-00`), both 5★ with icons,
  illustrations, and 1024×576 previews. Pinned upstream commits advanced to master data version
  `97f9d1d7…` (EN `b1f9535b…`, JP `4ab2389a…`).
- **Documented art exception** (user-approved): both new illustrations ship
  at their best-available originals (2099/2098 × 1179, below the 2282×1284
  floor) via an explicit exact-pinned exception ledger
  (`scripts/lib/illustration-floor-exceptions.mjs`), flagged for upgrade
  when a sanctioned source posts full resolution.
- **Songs/charts**: 177 → 182 songs, 708 → 728 aggregate charts. The chart
  timeline source API is now behind a Cloudflare challenge; per charter (no
  scrape-protection bypass) the 20 new charts and one upstream-revised chart
  (`m0318:easy`, stale timeline removed) are declared timeline-unavailable
  with a new schema-validated reason, riding the designed modeled-placement
  path until the source reopens. The frozen 30-chart ranking benchmark and
  all guide rating charts are unaffected.
- **Rankings**: all six lenses regenerated at 115 entries against the frozen
  benchmark; 325 attributed changes; both new cards enter tier B in the
  affected lenses; six frozen baselines and the public changelog updated.
- **Guides**: all seven regenerated on the same snapshot (guide-referenced
  member cards 76 → 79 — the new cards enter guide teams).
- **Sync hardening**: AppMedia article-thumbnail alts excluded from icon
  matching; single-match errors now print candidates; song/chart/timeline
  count pins advanced deliberately.
- **Copy audit**: 78 occurrences, zero violations, zero unresolved (new
  official skill text "Score Support Effect" classified); digest
  `2895b017…`.
- **Research-track honesty**: the exact-optimizer scope re-pinned to the
  115-card roster (new scope hash `2933f82f…`; legal team count
  126,445,821 → 137,891,541). All recorded v0.2 research evidence remains
  pinned to the previous 113-card scope and now reports as honestly stale
  against the current scope (coverage gates refuse authorization with
  specific reasons); `certificateEligible`/`fullRunAuthorized` remain false;
  regeneration belongs to the research track.
