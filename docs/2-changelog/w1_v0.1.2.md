# Changelog - Week 1, 04-08-2026, V. 0.1.2

**Release Date**: Week 1, 04-08-2026 at 20:28
**Version**: 0.1.2 (previously 0.1.0)
**Object**: feat(exact): complete X06 continuation-complete partial-state validation (v0.2 70%)
**Code review**: `docs/3-code-review/CR_w1_v0.1.2.md` (Codex loop, 2 rounds -> APPROVED with observations; takeover audit in `docs/3-code-review/CR_0.1.2_x06-takeover-audit.md`)

## Changes

- **Takeover audit** of the inherited uncommitted X06 exact-optimizer work
  (9 findings): fixed a `canonicalize` defect that made the bulk-performance
  evidence hash unreproducible from its own bytes and regenerated the
  perf → cost → dominance artifact chain with self-reproducible hashes;
  hardened `--rehash-existing` validation; corrected X05's self-attested
  independent-review wording.
- **B2 clamp-identity guard (D-5)**: the central-only fast path now computes
  all three lane contributions per certified run and verifies
  `lower ≤ central ≤ upper` before certifying, falling back to ordered
  replay on violation. Full bulk and compiled parity corpora re-executed:
  zero mismatches, stable evidence digest bit-identical (`320da206…`).
- **Continuation-complete fixed-Leader partial state** (X06 criterion 6):
  design document, `@yagoo-dori/core` module
  (`exact-optimizer-partial-state.ts`) with canonical binary64-bits state
  serialization, state-only suffix resumption, and pure-arithmetic
  accumulator checkpoint continuation; read-only run-ledger instrumentation
  (D-6, digest-gated additive).
- **Exhaustive reduced-scope validation** (`optimizer:suffix:validation`):
  56 legal sets × 4 Leaders × all 120 orders × depths 0–5 — 161,280
  byte-identical state resumptions, 633,660 mutation divergences, 2,321,280
  bit-identical accumulator boundary comparisons, zero mismatches; stable
  digest `3dbda663…` reproduced across two complete runs. Zero
  distinct-history merge pairs: resumption soundness proven, merge-rule
  soundness explicitly unclaimed.
- **Honest dominance disposition**: pilot remains `attempted:false` with
  zero pruning/timing/projection credit; reopening requirements 1–2
  satisfied for the reduced scope, the strict-loss prune proof outstanding.
  `certificateEligible=false` and `fullRunAuthorized=false` throughout; the
  864-shard plan remains unexecuted; no rankings, guides, or v0.1 public
  surface changes.
- **Status**: X06 done (6/6); v0.2 verified completion 65% → 70%; X04
  remains blocked by the cost gate; X07 pending. Continuation-completeness
  is proven at the declared merge boundaries per the user-ratified D-7
  amendment (independent-review dissent preserved in the CR).
