# Changelog - Week 1, 10-08-2026, V. 0.1.8

**Release Date**: Week 1, 10-08-2026
**Version**: 0.1.8 (previously 0.1.7)
**Object**: fix(board): per-talent tree models + orientation; feat: generation-order member listings; fix(trust): external-review triage batch
**Code review**: 29-finding external review triaged against HEAD with every disposition recorded in docs/3-code-review/CR_external-review-triage_2026-08-10.md; the sort-order feature implemented by Codex Luna and reviewed by Codex Sol to APPROVED (one Major fixed); board geometry fixes screenshot-verified against the user's in-game reference; full verification chain + copy audit green on the merged tree

## Changes

- **holomem Board — correct per-talent tree models (user-reported)**: the
  board rendered every talent on tree-model-001 and drew the grid
  vertically mirrored; Connect footprint gains also used model-001
  geometry for everyone. The pinned upstream `Character.json` maps each
  playable talent to its real tree model (15/14/13/12 across the four
  models — Usada Pekora is on tree-model-004), and grid coordinates are
  y-up. All 54 talents now render their own correctly oriented layout
  (Pekora: leader path up, card path right), Connect recommendations use
  each talent's real footprint geometry, and arrow-key navigation follows
  the same per-model, y-up directions. New `talentBoardProfiles` catalog
  (mechanics `yd-mechanics-catalog-1.2.0`; scope re-pinned and board-model
  evidence regenerated in the same commit); the talent-to-tree-model
  runtime assumption upgraded from unresolved default to verified catalog
  mapping. Board presentation moved closer to the game's look (dotted
  cell lattice, ring-style nodes).
- **Member listings in the game's generation order**: the pinned upstream
  `Character.order` field now flows through the public-data sync as
  `generationOrder` on all 115 cards, and a shared deterministic
  comparator orders the cards page, team-calculator grid, board-planner
  picker and seeds, synergies, skills related-card lists, and
  card-profile related cards — Sora/Roboco/AZKi first through ReGLOSS
  last, matching the in-game default instead of alphabetical order.
  Calculator input is re-canonicalized by card id before enumeration, so
  search and ranking semantics are unchanged.
- **External-review triage (29 findings, every one dispositioned)**:
  - the methodology no longer claims the central lane always resolves
    unresolved targets to the guaranteed recipient — it now names the
    real three-rule composition (floor for stat-effect targeting, plain
    average over enumerated Active application readings, independent
    expected maximum for overlap); copy-only, the deliberate code
    policies are untouched;
  - the tier list now discloses what it is: provisional model tiers
    against a frozen launch benchmark, with index ranges,
    matched-context counts and a provisional badge in quick view,
    explanations for the structurally empty SS/D bands (S is the current
    effective top), and a results-line marker — with the site-wide
    leak-guard e2e now requiring the disclosure on the tier page while
    banning the term elsewhere;
  - the calculator headline is "Model utility" with "not a projected
    Live Score" beside it and Board/Connect exclusion stated in the
    result header; the homepage no longer implies song-specific
    optimization; "Investment efficiency" renamed to "Progression
    value" with its model-state x-axis stated;
  - Board plan values are named modeled parameter value (not Live
    Score), the amplification model renders in plain language, connect
    tables use talent names and disclose in-range-but-unevaluated node
    counts, and both sides of the calculator import state that team
    choice and Board plan are separate recommendations;
  - a new exhaustive-vs-beam fixture measures the Board suggester at
    regret 0 against brute-force enumeration at small budgets;
  - sitemap.xml (static + cards + leaders + talents + guides), canonical
    + Open Graph + Twitter metadata, robots Sitemap directive when
    publication-ready, footer version display, extended live smoke
    (talents/methodology/holomem-board/sitemap), Pages deploy gate
    strengthened with assets:check and data:validate, historical banners
    on superseded review docs, ARCHI counts moved to the current
    115-card scope.
  - Deferred with measured reasons (owner-priority queue in the triage
    document): exact-timeline shadow comparison, ranking ablation, joint
    team+order validation, hand-calculated goldens, axe accessibility
    smoke, roster export/import, CI job split, sensitivity harness,
    methodology readability, sync-diff manifest, performance budgets.
    Owner decisions outstanding: tier-gate policy and repository
    licensing.
- **Copy audit**: 103 occurrences, zero violations, zero unresolved
  (two new allowed-scoped entries for the tier and Board not-Live-Score
  denials; one stale homepage entry removed with its sentence).
- **Research-track honesty**: certified exact-optimizer scope re-pinned
  for the mechanics catalog change only (generator scripts, never
  hand-edited); `certificateEligible`/`fullRunAuthorized` remain false;
  X04 remains blocked by the cost gate.
