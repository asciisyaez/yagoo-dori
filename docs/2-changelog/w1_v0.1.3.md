# Changelog - Week 1, 04-08-2026, V. 0.1.3

**Release Date**: Week 1, 04-08-2026 at 22:50
**Version**: 0.1.3 (previously 0.1.2)
**Object**: feat(exact): complete X07 scope-safe validation and public copy audit (v0.2 73%)
**Code review**: `docs/3-code-review/CR_w1_v0.1.3.md` (Codex loop, 2 rounds -> APPROVED, no overrides)

## Changes

- **Epic restructure**: the blocked X07 validation ticket split into the
  X04-independent X07 "Scope-safe validation and public copy audit"
  (weight 3, done) and the new X08 "Production certificate validation"
  (weight 2, pending behind X04), preserving every original criterion clause
  and the 100% weight total.
- **Negative-path regressions**: the exact-optimizer scope guards, coverage
  stale-artifact detectors, and joint-claim contract rejections now have
  specific negative tests with re-signed mutations, exercised through the
  same exported validation the production initializer calls.
- **Verifier hardening**: both shard verifiers read through a bounded JSON
  reader (pre-allocation byte cap via statSync, depth cap, prototype-key
  rejection) with byte-identical valid-path output; a 14-case
  mutation-rejection runner (`optimizer:verify:rejections`) proves each
  corruption fails with its specific error line.
- **Public copy audit**: new harness (`copy:audit`) scanning UI strings
  (including template quasis) and all rendered generated prose via
  path-heuristic discovery; 78 occurrences classified in an explicit
  disposition ledger with zero violations and zero unresolved; reproducible
  evidence artifact `data/native/public-copy-audit-v1.json` (digest
  `414cb388…`).
- **Approved claim-narrowing corrections**: the calculator's certified badge
  now names both the owned roster and the 30-chart benchmark (pinned
  verbatim in e2e), and guide pages render the generated
  `searchCertificate.caveat`.
- **Live smoke**: env-gated read-only probes of the deployed site
  (`smoke:live`): 5 routes HTTP 200, disclaimer exactly once outside script
  tags.
- **Status**: X07 done 3/3; v0.2 70% → 73%; flags remain false; X04 cost
  gate, X08 pending state, and the 864-shard prohibition unchanged; no
  rankings/guides regenerated; nothing pushed, tagged, or deployed.
